# config

## x shot 1 build a hal config chooser (2026-04-11 05:13:09)
there are a lot of yml config files in the global hal config folder at ~/.config/hal
they are serated by domains.
i want the command HalConfigPicker to open a telescope picker, which lists all the .yml files in the ~/.config/hal folder and subfolders and on enter opens it
map this command to < >C
